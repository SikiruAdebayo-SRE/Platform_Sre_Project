apiVersion: v1
kind: ConfigMap
metadata:
  name: mosquitto-config
  namespace: gridops-edge
data:
  mosquitto.conf: |
    listener 1883 0.0.0.0
    allow_anonymous false
    password_file /mosquitto/config/passwd
    max_inflight_messages 0
    max_queued_messages 10000
