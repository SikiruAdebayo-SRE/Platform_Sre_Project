apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-data
  namespace: n8n
spec:
  accessModes:
    - ReadWriteOnce  # Changed to RWO
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
